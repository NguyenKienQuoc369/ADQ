"use client";

import React from "react";

interface ThemeToggleProps {
  className?: string;
  showLabel?: boolean;
}

/**
 * ADQ Authenticated Application is Dark-Only.
 * User-facing theme switches are disabled.
 */
export function ThemeToggle(_props: ThemeToggleProps) {
  return null;
}

# Contributing to ADQ Enterprise DAST/ASM Platform 🛡️

Thank you for your interest in contributing to **ADQ**! We welcome bug hunters, security researchers, and software engineers to help build the ultimate open-source Application Security Testing & Continuous Threat Exposure Management platform.

---

## 🏛️ Monorepo Ownership & Code Boundaries

To maintain clean architecture in our Monorepo, responsibility is divided between Backend and Frontend domains:

* **Backend Domain (`backend/`):**
  * Business logic, Master-Worker orchestration, scan profiles, WAF evasion, OAST, and Knowledge Graph algorithms.
* **Frontend Domain (`frontend/`):**
  * Next.js App Router, Tailwind CSS, React components, state management, and real-time SSE stream consumption.
* **Shared Schema Contract (`backend/prisma/schema.prisma` & `frontend/prisma/schema.prisma`):**
  * The database schema is the single source of truth. Any schema modifications **require mutual consensus** between Backend and Frontend leads before submitting a PR.

---

## 📜 The API Contract

We enforce a strict **"No Spec-less Coding"** policy:

* **Interactive Swagger UI:** Live API documentation is automatically generated by FastAPI at `http://localhost:8000/docs`.
* Frontend developers can test endpoints, inspect expected query/body payloads, and view JSON response schemas directly from Swagger UI.

---

## 🛠️ Local Development Setup (Frontend "One-Click" Experience)

Frontend engineers do not need to configure Python or Go binaries locally. Run the backend as a blackbox service with Docker Compose:

1. **Start Backend Services:**
   ```bash
   docker compose up -d db redis api-server oast-server
   ```

2. **Run Web Dashboard in Development Mode:**
   ```bash
   cd frontend
   npm install
   npx prisma generate
   npm run dev
   ```
   The Next.js dashboard will run on `http://localhost:3000` connected to the live API on `http://localhost:8000`.

---

## 🌳 Git Flow & Branch Protection Rules

1. **`main` Branch is Sacred:**
   * Direct pushes to `main` are restricted. All changes must go through Pull Requests.
   * `main` must always remain buildable with 100% test pass rate (`pytest backend/tests/ -v`).

2. **Branch Naming Conventions:**
   * Features: `feat/feature-name` (e.g., `feat/ctem-dashboard`, `feat/graphql-parser`)
   * Fixes: `fix/bug-description` (e.g., `fix/table-scroll`, `fix/waf-backoff`)
   * Refactoring: `refactor/optimization-name` (e.g., `refactor/graph-algo`)

3. **Pull Request & Code Review Process:**
   * Create a PR targeting `main`.
   * Ensure GitHub Actions CI workflows pass (`core-testing-hardening.yml` & `docker-build-push.yml`).
   * Require at least 1 approval from a Tech Lead before merging.

Thank you for making ADQ stronger! 🚀

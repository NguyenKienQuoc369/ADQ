FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn

COPY host_telemetry.py /app/host_telemetry.py

CMD ["uvicorn", "host_telemetry:app", "--host", "0.0.0.0", "--port", "9100"]
